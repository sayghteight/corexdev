@extends('layouts.app')
@section('title', 'CoreXDev — Noticias de Videojuegos')

@section('content')

{{-- ── HERO SLIDER ─────────────────────────────────────────────────────── --}}
@if($sliderPosts->isNotEmpty())
<section class="relative h-[580px] overflow-hidden bg-[#060910]" x-data="slider({{ $sliderPosts->count() }})">
    {{-- Slides --}}
    @foreach($sliderPosts as $i => $post)
        <div class="absolute inset-0 transition-opacity duration-700"
             :class="current === {{ $i }} ? 'opacity-100 z-10' : 'opacity-0 z-0'">
            @if($post->image)
                <img src="{{ asset('storage/' . $post->image) }}" alt="{{ $post->title }}"
                     class="w-full h-full object-cover object-center scale-105 group-hover:scale-100 transition-transform duration-[8000ms]"
                     loading="{{ $i === 0 ? 'eager' : 'lazy' }}">
            @else
                <div class="w-full h-full" style="background:linear-gradient(135deg,#060910 0%,#0d1526 50%,#060910 100%)">
                    <div class="absolute inset-0 grid-bg opacity-30"></div>
                </div>
            @endif
            {{-- Multi-layer overlay for cinematic feel --}}
            <div class="absolute inset-0 slider-overlay"></div>
            <div class="absolute inset-0" style="background:linear-gradient(to top, rgba(6,9,16,.7) 0%, transparent 50%)"></div>

            <div class="absolute inset-0 flex items-center">
                <div class="max-w-7xl mx-auto px-6 w-full">
                    <div class="max-w-2xl" :class="current === {{ $i }} ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-4'"
                         style="transition: opacity .6s ease .1s, transform .6s ease .1s">
                        @if($post->category)
                            <span class="inline-flex items-center gap-2 px-3 py-1 text-[11px] font-extrabold uppercase tracking-widest rounded-full mb-4"
                                  style="background:{{ $post->category->color ?? '#00d4ff' }}20; color:{{ $post->category->color ?? '#00d4ff' }}; border:1px solid {{ $post->category->color ?? '#00d4ff' }}50">
                                <span class="w-1.5 h-1.5 rounded-full animate-pulse" style="background:{{ $post->category->color ?? '#00d4ff' }}"></span>
                                {{ $post->category->name }}
                            </span>
                        @endif
                        <h2 class="text-4xl md:text-[3.2rem] font-extrabold text-white leading-tight mb-4 hero-title-glow" style="line-height:1.1">
                            {{ $post->title }}
                        </h2>
                        <p class="text-gray-300/80 text-base mb-7 line-clamp-2 max-w-xl">{{ $post->excerpt }}</p>
                        <div class="flex items-center gap-4">
                            <a href="{{ route('posts.show', $post->slug) }}"
                               class="inline-flex items-center gap-2.5 px-7 py-3 font-bold text-sm uppercase tracking-wide rounded-full text-[#060910] transition-all"
                               style="background:linear-gradient(135deg,#00d4ff,#0099cc); box-shadow:0 0 20px rgba(0,212,255,.4)"
                               onmouseenter="this.style.boxShadow='0 0 32px rgba(0,212,255,.7)'"
                               onmouseleave="this.style.boxShadow='0 0 20px rgba(0,212,255,.4)'">
                                Leer artículo
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                            </a>
                            @if($post->published_at)
                                <span class="text-gray-500 text-xs">{{ $post->published_at->format('d M Y') }}</span>
                            @endif
                        </div>
                    </div>
                </div>
            </div>
        </div>
    @endforeach

    {{-- Arrows --}}
    <button @click="prev()" class="absolute left-4 top-1/2 -translate-y-1/2 z-20 w-11 h-11 rounded-full text-white/80 hover:text-white flex items-center justify-center transition-all"
            style="background:rgba(0,0,0,.5); border:1px solid rgba(255,255,255,.08); backdrop-filter:blur(4px)"
            onmouseenter="this.style.background='rgba(0,212,255,.15)'; this.style.borderColor='rgba(0,212,255,.4)'"
            onmouseleave="this.style.background='rgba(0,0,0,.5)'; this.style.borderColor='rgba(255,255,255,.08)'">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M15 19l-7-7 7-7"/></svg>
    </button>
    <button @click="next()" class="absolute right-4 top-1/2 -translate-y-1/2 z-20 w-11 h-11 rounded-full text-white/80 hover:text-white flex items-center justify-center transition-all"
            style="background:rgba(0,0,0,.5); border:1px solid rgba(255,255,255,.08); backdrop-filter:blur(4px)"
            onmouseenter="this.style.background='rgba(0,212,255,.15)'; this.style.borderColor='rgba(0,212,255,.4)'"
            onmouseleave="this.style.background='rgba(0,0,0,.5)'; this.style.borderColor='rgba(255,255,255,.08)'">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/></svg>
    </button>

    {{-- Dots --}}
    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 z-20 flex gap-1.5">
        @foreach($sliderPosts as $i => $post)
            <button @click="current = {{ $i }}"
                    :class="current === {{ $i }} ? 'w-8 opacity-100' : 'w-2 opacity-40 hover:opacity-70'"
                    class="h-1.5 rounded-full transition-all duration-400"
                    style="background:#00d4ff"></button>
        @endforeach
    </div>
</section>
@endif

{{-- ── PRÓXIMOS EVENTOS (countdown bar) ─────────────────────────────────── --}}
@if($upcomingEvents->isNotEmpty())
<div class="bg-[#060910] border-b border-[#1e2a3a]/60" style="background:linear-gradient(180deg, #060910 0%, #08060f 100%)">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex items-center gap-0 overflow-x-auto py-0 scrollbar-thin">
            <div class="flex-shrink-0 flex items-center gap-2 pr-4 py-3 border-r border-[#1e2a3a]">
                <span class="relative flex h-2 w-2">
                    <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-[#00d4ff] opacity-75"></span>
                    <span class="relative inline-flex rounded-full h-2 w-2 bg-[#00d4ff]"></span>
                </span>
                <span class="text-[#00d4ff] font-extrabold text-[10px] uppercase tracking-[.2em] whitespace-nowrap">Próximos</span>
            </div>
            <div class="flex items-center gap-3 px-4 py-3 overflow-x-auto overflow-y-hidden">
                @foreach($upcomingEvents as $event)
                    <a href="{{ route('events.show', $event->slug) }}"
                       class="flex-shrink-0 flex items-center gap-2.5 rounded-lg px-3 py-2 transition-all group min-w-[160px]"
                       style="background:rgba(17,24,39,.8); border:1px solid #1e2a3a"
                       onmouseenter="this.style.borderColor='rgba(0,212,255,.3)'"
                       onmouseleave="this.style.borderColor='#1e2a3a'">
                        @if($event->image)
                            <img src="{{ asset('storage/' . $event->image) }}" alt="{{ $event->name }}"
                                 class="w-8 h-8 object-cover rounded flex-shrink-0 opacity-80 group-hover:opacity-100 transition-opacity">
                        @else
                            <div class="w-8 h-8 rounded flex items-center justify-center text-sm flex-shrink-0" style="background:#1e2a3a">🎮</div>
                        @endif
                        <div class="min-w-0">
                            <p class="text-white text-xs font-semibold truncate group-hover:text-[#00d4ff] transition-colors">{{ $event->name }}</p>
                            <p class="text-[#00d4ff] text-[10px] font-mono leading-tight" x-data x-text="countdown('{{ $event->event_date->toISOString() }}')">—</p>
                        </div>
                    </a>
                @endforeach
            </div>
        </div>
    </div>
</div>
@endif

{{-- ── NOTICIAS RECIENTES ─────────────────────────────────────────────── --}}
<section class="max-w-7xl mx-auto px-4 py-12">
    <div class="flex items-end justify-between mb-7">
        <div>
            <p class="section-label mb-2">Lo más reciente</p>
            <h2 class="text-white text-2xl font-extrabold tracking-tight">Noticias de Videojuegos</h2>
        </div>
        <a href="{{ route('posts.index') }}"
           class="hidden sm:flex items-center gap-1.5 text-[#00d4ff] text-xs font-bold hover:gap-3 transition-all uppercase tracking-wide">
            Ver todo
            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
        </a>
    </div>

    @if($latestPosts->isNotEmpty())
        {{-- Editorial layout: first post is large --}}
        @php $firstPost = $latestPosts->first(); $restPosts = $latestPosts->skip(1); @endphp
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-5">
            {{-- Featured large card --}}
            <a href="{{ route('posts.show', $firstPost->slug) }}"
               class="lg:col-span-5 relative rounded-2xl overflow-hidden group card-hover flex flex-col min-h-[380px]"
               style="border:1px solid #1e2a3a"
               onmouseenter="this.style.borderColor='rgba(0,212,255,.2)'"
               onmouseleave="this.style.borderColor='#1e2a3a'">
                @if($firstPost->image)
                    <img src="{{ asset('storage/' . $firstPost->image) }}" alt="{{ $firstPost->title }}"
                         class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-700">
                @else
                    <div class="absolute inset-0" style="background:linear-gradient(135deg,#0d1526,#111827)">
                        <div class="absolute inset-0 grid-bg opacity-20"></div>
                    </div>
                @endif
                <div class="absolute inset-0" style="background:linear-gradient(to top, rgba(6,9,16,.98) 0%, rgba(6,9,16,.5) 50%, transparent 100%)"></div>
                <div class="relative mt-auto p-6">
                    @if($firstPost->category)
                        <span class="inline-block px-2.5 py-0.5 text-[10px] font-extrabold uppercase tracking-wider rounded-full mb-3"
                              style="background:{{ $firstPost->category->color ?? '#00d4ff' }}20; color:{{ $firstPost->category->color ?? '#00d4ff' }}; border:1px solid {{ $firstPost->category->color ?? '#00d4ff' }}40">
                            {{ $firstPost->category->name }}
                        </span>
                    @endif
                    <h3 class="text-white font-extrabold text-xl leading-tight group-hover:text-[#00d4ff] transition-colors line-clamp-3">
                        {{ $firstPost->title }}
                    </h3>
                    @if($firstPost->excerpt)
                        <p class="text-gray-400 text-xs mt-2 line-clamp-2">{{ $firstPost->excerpt }}</p>
                    @endif
                    <div class="flex items-center gap-3 mt-4 text-gray-500 text-xs">
                        <span>{{ $firstPost->published_at?->diffForHumans() }}</span>
                        <span class="flex items-center gap-1">
                            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
                            {{ number_format($firstPost->views) }}
                        </span>
                    </div>
                </div>
            </a>

            {{-- Grid of remaining posts --}}
            <div class="lg:col-span-7 grid grid-cols-1 sm:grid-cols-2 gap-5">
                @foreach($restPosts->take(6) as $post)
                    @include('components.post-card', ['post' => $post])
                @endforeach
            </div>
        </div>
    @else
        <div class="text-center py-20">
            <p class="text-5xl mb-3 opacity-30">📰</p>
            <p class="text-gray-500">No hay noticias disponibles todavía.</p>
        </div>
    @endif
</section>

{{-- ── GUÍAS Y RESEÑAS (lado a lado) ───────────────────────────────────── --}}
<section class="max-w-7xl mx-auto px-4 pb-12 grid grid-cols-1 lg:grid-cols-2 gap-8">

    {{-- Guías --}}
    <div class="rounded-2xl overflow-hidden" style="background:#0d111d; border:1px solid #1e2a3a">
        <div class="px-5 py-4 border-b border-[#1e2a3a] flex items-center justify-between">
            <div class="flex items-center gap-2">
                <div class="w-1 h-5 rounded-full" style="background:linear-gradient(180deg,#8b5cf6,#6d28d9)"></div>
                <h2 class="text-white font-extrabold">Guías</h2>
            </div>
            <a href="{{ route('guides.index') }}" class="text-[#8b5cf6] text-xs font-bold hover:underline">Ver todo →</a>
        </div>
        <div class="p-4 space-y-2">
            @forelse($latestGuides as $post)
                <a href="{{ route('posts.show', $post->slug) }}"
                   class="flex gap-3 rounded-xl p-3 group transition-all"
                   style="background:rgba(255,255,255,.02); border:1px solid transparent"
                   onmouseenter="this.style.background='rgba(139,92,246,.05)'; this.style.borderColor='rgba(139,92,246,.2)'"
                   onmouseleave="this.style.background='rgba(255,255,255,.02)'; this.style.borderColor='transparent'">
                    <div class="w-16 h-16 rounded-lg overflow-hidden flex-shrink-0">
                        @if($post->image)
                            <img src="{{ asset('storage/' . $post->image) }}" alt="{{ $post->title }}"
                                 class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500">
                        @else
                            <div class="w-full h-full flex items-center justify-center text-2xl" style="background:#1a1f2e">📖</div>
                        @endif
                    </div>
                    <div class="min-w-0 flex flex-col justify-center">
                        @if($post->category)
                            <span class="text-[10px] font-bold uppercase tracking-wide" style="color:#8b5cf6">{{ $post->category->name }}</span>
                        @endif
                        <p class="text-white/90 text-sm font-semibold leading-snug group-hover:text-[#8b5cf6] transition-colors line-clamp-2">{{ $post->title }}</p>
                        <p class="text-gray-600 text-xs mt-0.5">{{ $post->published_at?->diffForHumans() }}</p>
                    </div>
                </a>
            @empty
                <p class="text-gray-600 text-sm py-6 text-center">No hay guías todavía.</p>
            @endforelse
        </div>
    </div>

    {{-- Reseñas --}}
    <div class="rounded-2xl overflow-hidden" style="background:#0d111d; border:1px solid #1e2a3a">
        <div class="px-5 py-4 border-b border-[#1e2a3a] flex items-center justify-between">
            <div class="flex items-center gap-2">
                <div class="w-1 h-5 rounded-full" style="background:linear-gradient(180deg,#ffd700,#f59e0b)"></div>
                <h2 class="text-white font-extrabold">Reseñas</h2>
            </div>
            <a href="{{ route('reviews.index') }}" class="text-[#ffd700] text-xs font-bold hover:underline">Ver todo →</a>
        </div>
        <div class="p-4 space-y-2">
            @forelse($latestReviews as $review)
                <a href="{{ route('reviews.show', $review->slug) }}"
                   class="flex gap-3 rounded-xl p-3 group transition-all"
                   style="background:rgba(255,255,255,.02); border:1px solid transparent"
                   onmouseenter="this.style.background='rgba(255,215,0,.04)'; this.style.borderColor='rgba(255,215,0,.2)'"
                   onmouseleave="this.style.background='rgba(255,255,255,.02)'; this.style.borderColor='transparent'">
                    <div class="w-16 h-16 rounded-lg overflow-hidden flex-shrink-0">
                        @if($review->image)
                            <img src="{{ asset('storage/' . $review->image) }}" alt="{{ $review->title }}"
                                 class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500">
                        @else
                            <div class="w-full h-full flex items-center justify-center text-2xl" style="background:#1a1f2e">🎮</div>
                        @endif
                    </div>
                    <div class="flex-1 min-w-0 flex flex-col justify-center">
                        <span class="text-[10px] font-bold uppercase tracking-wide text-[#ffd700]">{{ $review->game }}</span>
                        <p class="text-white/90 text-sm font-semibold leading-snug group-hover:text-[#ffd700] transition-colors line-clamp-2">{{ $review->title }}</p>
                        <p class="text-gray-600 text-xs mt-0.5">{{ $review->published_at?->diffForHumans() }}</p>
                    </div>
                    {{-- Score bubble --}}
                    <div class="flex-shrink-0 w-11 h-11 rounded-full flex items-center justify-center font-black text-sm"
                         style="background:rgba(255,215,0,.1); border:2px solid rgba(255,215,0,.5); color:#ffd700">
                        {{ $review->score }}
                    </div>
                </a>
            @empty
                <p class="text-gray-600 text-sm py-6 text-center">No hay reseñas todavía.</p>
            @endforelse
        </div>
    </div>
</section>
                    <div class="flex-1 min-w-0">
                        <span class="text-xs text-[#ffd700] font-semibold uppercase">{{ $review->game }}</span>
                        <p class="text-white text-sm font-semibold leading-snug group-hover:text-[#ffd700] transition-colors line-clamp-2">{{ $review->title }}</p>
                        <p class="text-gray-500 text-xs mt-1">{{ $review->published_at?->diffForHumans() }}</p>
                    </div>
                    <div class="flex-shrink-0 flex items-center justify-center w-10 h-10 rounded-full bg-[#ffd700]/10 border border-[#ffd700]/30 text-[#ffd700] text-sm font-black">
                        {{ $review->score }}
                    </div>
                </a>
            @empty
                <p class="text-gray-500 text-sm py-4">No hay reseñas todavía.</p>
            @endforelse
        </div>
    </div>
</section>

{{-- ── SORTEOS
        <div class="grid grid-cols-1 md:grid-cols-3 gap-5">
            @foreach($activeGiveaways as $giveaway)
                <a href="{{ route('giveaways.show', $giveaway->slug) }}"
                   class="rounded-2xl overflow-hidden flex flex-col group card-hover"
                   style="background:#111827; border:1px solid rgba(255,107,0,.25)"
                   onmouseenter="this.style.borderColor='rgba(255,107,0,.6)'; this.style.boxShadow='0 0 24px rgba(255,107,0,.12)'"
                   onmouseleave="this.style.borderColor='rgba(255,107,0,.25)'; this.style.boxShadow='none'">
                    <div class="relative overflow-hidden">
                        @if($giveaway->image)
                            <img src="{{ asset('storage/' . $giveaway->image) }}" alt="{{ $giveaway->title }}"
                                 class="w-full h-44 object-cover group-hover:scale-105 transition-transform duration-700">
                        @else
                            <div class="w-full h-44 flex items-center justify-center text-5xl"
                                 style="background:linear-gradient(135deg,rgba(255,107,0,.12),rgba(255,215,0,.08))">🎁</div>
                        @endif
                        <div class="absolute inset-0" style="background:linear-gradient(to top, rgba(17,24,39,.9) 0%, transparent 60%)"></div>
                        <div class="absolute top-3 right-3">
                            <span class="px-2.5 py-1 text-[10px] font-black uppercase tracking-wider rounded-full text-white"
                                  style="background:rgba(255,107,0,.9); backdrop-filter:blur(4px)">EN CURSO</span>
                        </div>
                    </div>
                    <div class="p-5 flex flex-col flex-1">
                        <h3 class="text-white font-bold text-sm leading-snug group-hover:text-[#ff6b00] transition-colors line-clamp-2 flex-1">
                            {{ $giveaway->title }}
                        </h3>
                        <div class="mt-3 pt-3 border-t border-[#1e2a3a]">
                            <div class="flex items-center gap-2 mb-1">
                                <svg class="w-3.5 h-3.5 text-[#ffd700]" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M5 5a3 3 0 015-2.236A3 3 0 0114.83 6H16a2 2 0 010 4h-5V9a1 1 0 10-2 0v1H4a2 2 0 010-4h1.17C5.06 5.687 5 5.35 5 5zm4 1V5a1 1 0 10-1 1h1zm3 0a1 1 0 10-1-1v1h1z" clip-rule="evenodd"/><path d="M9 11H3v5a2 2 0 002 2h4v-7zM11 18h4a2 2 0 002-2v-5h-6v7z"/></svg>
                                <span class="text-[#ffd700] text-xs font-semibold">{{ $giveaway->prize }}</span>
                            </div>
                            <p class="text-gray-600 text-xs">Termina: {{ $giveaway->end_date->format('d M Y') }}</p>
                        </div>
                    </div>
                </a>
            @endforeach
        </div>
    </div>
</section>
@endif

@endsection

@push('scripts')
<script>
function slider(total) {
    return {
        current: 0,
        timer: null,
        init() {
            this.timer = setInterval(() => this.next(), 5500);
        },
        next() { this.current = (this.current + 1) % total; },
        prev() { this.current = (this.current - 1 + total) % total; },
    };
}

function countdown(isoDate) {
    const target = new Date(isoDate);
    const diff = target - new Date();
    if (diff <= 0) return 'Ya disponible';
    const d = Math.floor(diff / 86400000);
    const h = Math.floor((diff % 86400000) / 3600000);
    const m = Math.floor((diff % 3600000) / 60000);
    return `${d}d ${h}h ${m}m`;
}
</script>
@endpush
